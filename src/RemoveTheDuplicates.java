import java.util.Arrays;
import java.util.List;
import java.util.Set;
import java.util.stream.Collectors;
import java.util.stream.Stream;

public class RemoveTheDuplicates {

    public static void main(String[] args) {
 int arr[]=new int[]{12,13,24,25,12,47,24};
       // List<Integer> input = (List<Integer>) Arrays.asList(arr);
      //  Set<int[]> collect = Stream.of(arr).collect(Collectors.toSet()).stream().

        /*Stream().of.collect(Collectors.toSet())
                .forEach(x-> System.out.println(x));
        //System.out.println(collect);
        System.out.println("=============###==============");*/

        //input.stream().distinct().forEach(x-> System.out.println(x));


    }
}
